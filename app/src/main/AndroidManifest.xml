<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.otwfoods">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OTWFoods">
        <activity android:name=".Rider.SignInAsRiderActivity"></activity>
        <activity android:name=".RestaurantUser.Activities.MainAdminActivity" />
        <activity android:name=".Administrator.Activities.AddRestaurantActivity" />
        <activity android:name=".Administrator.Activities.SignInActivityAppAdmin" />
        <activity android:name=".RiderUser.RiderSignInActivity" />
        <activity android:name=".RestaurantUser.RestaurantSigninActivity" />
        <activity android:name=".Client.Activities.SignInActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>